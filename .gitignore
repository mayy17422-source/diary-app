# 依赖包（不上传，部署时重新安装）
node_modules/
client/node_modules/
server/node_modules/

# 构建产物
client/dist/
client/build/

# 环境变量（包含敏感信息，不上传）
.env
.env.local
.env.production

# 日志文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 系统文件
.DS_Store
Thumbs.db

# VS Code 配置（可选，按需保留）
# .vscode/
